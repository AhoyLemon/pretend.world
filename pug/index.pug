- var testing = true
- var siteTitle = "PRETEND 🌐 WORLD"
- var siteURL = ""
- var fPlusURL = false
- var description = ""
- var d = new Date()
- var lastUpdated = d.getFullYear() + '-' + (d.getMonth()<10?'0':'') + d.getMonth() + '-' + (d.getDate()<10?'0':'') + d.getDate() + '@' + (d.getHours()<10?'0':'') + d.getHours() + ':' + (d.getMinutes()<10?'0':'') + d.getMinutes() + ':' + (d.getSeconds()<10?'0':'') + d.getSeconds()

doctype html
html(lang="en")
  head
    include partials/_head.pug
  body

    main#app

      if testing == true
        include partials/_debug.pug

      figure.cheese-photo(style="background-image:url(img/cheeselog/1.jpg)" v-if="phase == 'showCheese'")
      figure.celebrity-photo(:style=" { backgroundImage: 'url('+current.pic+')' } " v-else)


      figcaption(v-if="phase == 'showCheese'")
        .inner.mission
          .status 
            h2 Look at that

            p These parties always have impressive spreads that spared no expense; figs from the middle east, the fresh caviar from Russia, and freshly baked pastries from France. All garbage for peasants. This cheese log is the true treasure.

            h1.mission-start
              a.big-text(@click="phase = 'question'") Mission Start!

      figcaption(v-else)
        include partials/_question.pug
        include partials/_answer.pug

    include partials/_javascripts.pug