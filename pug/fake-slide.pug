- var testing = true
- var siteTitle = "PRETEND 🌐 WORLD"
- var siteURL = "https://pretend.world"
- var fPlusURL = false
- var description = "You are at a party of celebrity impersonators."
- var d = new Date()
- var lastUpdated = d.getFullYear() + '-' + (d.getMonth()<10?'0':'') + d.getMonth() + '-' + (d.getDate()<10?'0':'') + d.getDate() + '@' + (d.getHours()<10?'0':'') + d.getHours() + ':' + (d.getMinutes()<10?'0':'') + d.getMinutes() + ':' + (d.getSeconds()<10?'0':'') + d.getSeconds()

doctype html
html(lang="en")
  head
    include partials/_head.pug
    link(href="css/deleteme.css?lastUpdated="+lastUpdated rel="stylesheet")
  body

    #app

      if testing == true
        //include partials/_debug.pug

      include partials/_sidebar.pug

      main(v-cloak)

        h1 A peasant.
      
      include partials/_banner.pug

    include partials/_javascripts.pug
    include partials/_schema.pug